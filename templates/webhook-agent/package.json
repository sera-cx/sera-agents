{
  "name": "sera-template-webhook-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Template: HTTP webhook responder powered by the Sera MCP.",
  "scripts": {
    "start": "tsx server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "audit": "npm audit --omit=dev --audit-level=high"
  },
  "dependencies": {
    "@openai/agents": "^0.3.0",
    "express": "^4.21.0",
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0",
    "@types/express": "^4.17.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
