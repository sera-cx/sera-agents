{
  "name": "sera-template-withdraw-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Template: terminal walkthrough of Sera's 4-step dual-signature instant-withdrawal flow. Demonstrates the withdraw tools added in sera-mcp v0.6.0.",
  "scripts": {
    "start": "tsx agent.ts",
    "typecheck": "tsc --noEmit",
    "audit": "npm audit --omit=dev --audit-level=high"
  },
  "dependencies": {
    "@openai/agents": "^0.3.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
