{
  "name": "sera-template-web-chat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Template: browser chat UI over the Sera MCP.",
  "scripts": {
    "start": "tsx server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "audit": "npm audit --omit=dev --audit-level=high"
  },
  "dependencies": {
    "@openai/agents": "^0.3.0",
    "express": "^5.2.1",
    "helmet": "^8.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.9"
  }
}
