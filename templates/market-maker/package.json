{
  "name": "sera-template-market-maker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Template: two-sided spread market-making bot on Sera. Demonstrates the maker tool surface (place_order, cancel_all_orders, multi_source_mid) added in sera-mcp v0.7.0.",
  "scripts": {
    "start": "tsx agent.ts",
    "typecheck": "tsc --noEmit",
    "audit": "npm audit --omit=dev --audit-level=high"
  },
  "dependencies": {
    "@openai/agents": "^0.3.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
